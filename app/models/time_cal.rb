class TimeCalc
  def initialize(event)
    @event = event
  end

  # def detect_event_status
  #   date_time_stamp = Date.new(@event.date).to_time.to_i
  #   date_now = DateTime.now()
  #   dateNow = Date.new(date_now).to_time.to_i
  #   if dateNow > date_time_stamp
  #     puts "Active"
  #   else
  #     puts "Past"
  #   end
  # end
end
