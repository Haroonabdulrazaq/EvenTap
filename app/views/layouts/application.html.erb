<!DOCTYPE html>
<html>
  <head>
    <title>EvenTap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <%= render 'shared/navigation'  %>
  <% if flash[:notice]%>
        <p class="notification is-primary has-text-white"><%= notice %></p>
      <% end %>
  <% if flash[:alert]%>
      <p class="notification is-danger has-text-white"><%= alert %></p>
  <% end %>
    <%= yield %>
  </body>
</html>
